__pycache__/
*.py[cod]
.venv/
.venv-test/
venv/
.ipynb_checkpoints/

# Large or regenerable. Result CSVs and their .meta.json sidecars ARE tracked:
# they are small, and they are the record of what was measured.
*.wav
*.flac
data/
checkpoints/
# figures/ is tracked: the paper's figures (PDF) and the PNG renders the README
# and RESULTS.md embed. `make figures` regenerates the PNG set.

.DS_Store

# Never commit: holds API tokens supplied at runtime.
env.sh
.dunya_token
infra/identity.txt
