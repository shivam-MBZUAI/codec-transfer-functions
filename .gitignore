__pycache__/
*.py[cod]
.venv/
.venv-test/
venv/
.ipynb_checkpoints/

# Large or regenerable. Result CSVs and their .meta.json sidecars ARE tracked:
# they are small, and they are the record of what was measured.
*.wav
*.flac
data/
checkpoints/
# figures/*.png are tracked: they are small, the README embeds one, and they
# are the exact images the paper uses. `make figures` regenerates them.

.DS_Store

# Never commit: holds API tokens supplied at runtime.
env.sh
.dunya_token
